package com.test;

//import org.springframework.context.ApplicationContext;
//import org.springframework.context.support.ClassPathXmlApplicationContext;

public class MainApp {
    public static void main(String[] args) {
        /*ApplicationContext context =
                new ClassPathXmlApplicationContext("applicationContext.xml");

        TaskDispatcher dispatcher1 = (TaskDispatcher) context.getBean("taskDispatcher");
        dispatcher1.executeAllTasks();

        dispatcher1.setDispatcherName("dispatcher 1");
        System.out.println("Dispatcher1 name: " + dispatcher1.getDispatcherName());

        TaskDispatcher dispatcher2 = (TaskDispatcher) context.getBean("taskDispatcher");
        dispatcher2.executeAllTasks();

        System.out.println("Dispatcher2 name: " + dispatcher2.getDispatcherName());*/
    }
}
